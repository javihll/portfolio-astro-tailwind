---
interface Props {
  title: string;
}

const { title = 'Tu Nombre' } = Astro.props;

interface NavItem {
  text: string;
  href: string;
}

const navItems: NavItem[] = [
  { text: 'Inicio', href: '#' },
  { text: 'Sobre mí', href: '#about' },
  { text: 'Proyectos', href: '#projects' },
  { text: 'Habilidades', href: '#skills' },
  { text: 'Contacto', href: '#contact' },
];
---


<header class="bg-white shadow-md sticky top-0 z-50">
  <nav class="container mx-auto px-4 py-4 flex justify-between items-center">
    <h1 class="text-2xl font-bold">{title}</h1>
    <ul class="flex space-x-4">
      {navItems.map((item: NavItem) => (
        <li><a href={item.href} class="text-gray-600 hover:text-blue-500 transition-colors duration-300">{item.text}</a></li>
      ))}
    </ul>
  </nav>
</header>

<style>
  /* Aquí puedes agregar estilos específicos para este componente si es necesario */
</style>
